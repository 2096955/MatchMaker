flask==3.1.0
flask-cors==5.0.1
psycopg[binary]==3.3.4  # console DB driver (Aurora PostgreSQL), used by db.py
pandas==2.2.3
openpyxl==3.1.5
xlrd==2.0.1
pyarrow==18.1.0
lxml==5.3.0
python-dotenv==1.0.1
pydantic>=2,<3
scipy>=1.16,<2
falkordb>=1.0.8
boto3>=1.34  # M8 S3 frame source; lazy-imported so mock-only runs don't require it
strands-agents>=0.1  # Bedrock-backed SCUDO agent runtime
mcp>=1.2.0   # FastMCP servers (ingestion_mcp / match_verify_mcp / persistence_mcp)
gunicorn>=22.0  # Flask production server with gthread workers for SSE
requests-aws4auth>=1.2  # NeptuneStore SigV4 SPARQL client
requests>=2.32  # scudo_mapping_mcp/url_ingest.py's URL-ingestion fetch (previously only a transitive dep of requests-aws4auth)
rdflib>=7.0.0
pyshacl>=0.26.0
urllib3>=2.0
cryptography>=44  # Ed25519 protected-evaluator envelope verification/signing
