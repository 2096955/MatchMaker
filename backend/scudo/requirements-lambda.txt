strands-agents>=1.42
pydantic>=2
scipy>=1.16,<2
mcp>=1.0
boto3>=1.34
falkordb>=1.0
# Azure OpenAI specialist+verifier path (AzureOpenAIShim in lambda_handler.py).
# Required whenever SCUDO_AGENT_PROVIDER_DEFAULT=azure or a /run payload sets
# agent_provider=azure — the shim does `from openai import AzureOpenAI`.
openai>=1.0
cryptography>=44
